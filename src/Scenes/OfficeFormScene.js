import 'phaser';
import Resources from '../Config/resources';
import Config from '../Config/config';
import UI from '../Ui/UiElements.js';

export default class OfficeFormScene extends Phaser.Scene {
    constructor() {
        super(Config.scenes.OfficeForm);
    }

    init() {}
    
    preload() {}

    create() {}
};
